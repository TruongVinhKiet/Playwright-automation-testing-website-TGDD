{
  "name": "lazada-playwright-automation",
  "version": "1.0.0",
  "description": "Lazada Playwright Automation Test",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "demo": "npx playwright test && npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.11.24"
  }
}